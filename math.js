//Generate a decimal number between 0 and 0.99
Math.random();

// save it to a variable
var decimal = Math.random();

//log the value of decimal to console
console.log(decimal);

//log the value of decimal MULTIPLIED by 10
console.log(decimal *10)

// this will round to the next number
Math.ceil();

var rounded = Math.ceil(1.01);
rounded = Math.ceil(2.003)
console.log(rounded)

